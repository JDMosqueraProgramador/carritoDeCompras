import { SubitFotosDirective } from './subit-fotos.directive';

describe('SubitFotosDirective', () => {
  it('should create an instance', () => {
    const directive = new SubitFotosDirective();
    expect(directive).toBeTruthy();
  });
});
